// A simple code to order generc number sequencies in ascending order
//include libs
#include <stdio.h>
#include <stdlib.h>

//include main function
int main (){
    //create a vector to contain the numbers
    int number[10];

    //loop to incert numbers
    for (int i = 1; i < 11; i++){
        //Ask for que number on current position
        printf ("please the %d° number", &i);
        scanf  ("%d", &number[i]); 
        }
    }